<p style="color: green"><%= notice %></p>

<div class="container ">
  <h1>Movies</h1>
  <%= link_to "New movie", new_movie_path, class: "btn btn-sm btn-secondary btn-outline-light" %>
  <div id="movies mt-3">
    <div class="row">
      <% @movies.each do |movie| %>
        <%= render movie %>
      <% end %>
    </div>
  </div>
</div>